{
  "name": "@oxgraph/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "oxgraph": "./dist/bin/oxgraph.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev:api": "tsc -p tsconfig.json && node dist/bin/oxgraph.js --api-only --no-open",
    "test": "vitest run"
  },
  "dependencies": {
    "@oxgraph/core": "workspace:*",
    "open": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@types/supertest": "^7.2.0",
    "supertest": "^7.2.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
